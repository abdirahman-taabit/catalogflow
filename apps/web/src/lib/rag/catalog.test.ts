import {describe,expect,it} from "vitest"; import {retrieveRows} from "./catalog";
describe("catalog retrieval",()=>{it("retrieves weak descriptions",()=>{const r=retrieveRows("Which products have missing descriptions?");expect(r.slice(0,2).map(x=>x.sku)).toEqual(expect.arrayContaining(["KTL-2016","BAG-2021"]));});it("retrieves travel products",()=>{const r=retrieveRows("Which travel products can be bundled?");expect(r.slice(0,3).map(x=>x.sku)).toContain("CAM-2006");expect(r.slice(0,3).map(x=>x.sku)).toContain("TRI-2007");});it("is deterministic and bounded",()=>{expect(retrieveRows("audio")).toHaveLength(6);expect(retrieveRows("audio")).toEqual(retrieveRows("audio"));});});
